generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("RAILWAY_DATABASE_URL")
}

model Todo {
  id      String   @id @default(cuid())
  title   String
  date    DateTime
  content String?
}
